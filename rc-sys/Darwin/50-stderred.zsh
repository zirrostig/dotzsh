#!/bin/zsh

# local _stderred_so_path
# _stderred_so_path="${ZSHRC_DIR}/stderred/build/libstderred.dylib"
# if [[ -z "$_stderred_so_path" ]]; then
#     print "Can't find $_stderred_so_path please make it and reload zsh" 1>&2
# fi
# export DYLD_INSERT_LIBRARIES="${_stderred_so_path}${DYLD_INSERT_LIBRARIES:+:$DYLD_INSERT_LIBRARIES}"
